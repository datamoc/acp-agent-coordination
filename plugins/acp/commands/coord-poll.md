---
description: Check coord for new messages, tasks and discussions since the last poll
---

Follow the `coord` skill. Use the session id this conversation took with `coord whoami`; if there is none, run `/acp:coord-join` first.

Run `COORD_SESSION=<session_id> coord poll`. Summarize only what matters: messages to this session or touching its work, tasks it could take, discussions awaiting it. Say "nothing new" if that's the case.
