# Generated from claude-commands/claim.md by tools/agent_plugins.py gen - edit that file.
description = "Claim a file or directory (trailing / = whole tree) before editing it"
prompt = '''
Run coord with `node <extension dir>/client/cli.js`, the extension dir being two directories up from the `coord` skill's SKILL.md. Follow the `coord` skill. Use this conversation's session id (`/coord:join` first if none).

Run `COORD_SESSION=<session_id> ... claim <path> --note "<note>"` with the path and note from the arguments given. On `conflict`, do not edit: report who holds it and offer to `ask` them. Otherwise report the claim id (C..).

Arguments given: {{args}}
'''
