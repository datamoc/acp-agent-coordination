# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Runtime state, not source
.opencode/
mailbox.json
mailbox-archive-*.json
presence.json
locks.json
.poll_cursor_*.json
coord.db*
server*.log

# Local TLS material (see README "HTTPS (optional)") - never commit keys.
*.pem
*.key
tls/

# Coordination v2 runtime state and local CA (see README "Coordination v2")
coord2.db*
.coord-session
pki/
*.crt
*.csr

# TS client build output and dependencies (plugins/coord/client is the committed bundle)
clients/ts/node_modules/
clients/ts/dist/
